---
title: "Início Rápido: Integre com a API do Brudam TMS em 10 minutos"
sidebarTitle: "Quickstart"
description: "Aprenda como autenticar na API do Brudam TMS e fazer sua primeira chamada com exemplos prontos para copiar e executar diretamente no terminal."
---

Esta página guia você pelos passos essenciais para realizar sua primeira chamada à API do Brudam TMS. Em menos de 10 minutos, você estará autenticado e consultando dados reais da sua operação de transporte.

## Passo a passo

<Steps>
  <Step title="Criar conta">
    Acesse [app.brudam.com.br](https://app.brudam.com.br) e crie sua conta gratuitamente. Você tem **7 dias de teste grátis** sem precisar cadastrar cartão de crédito — basta preencher os dados básicos da sua transportadora e começar a explorar.
  </Step>

  <Step title="Gerar token de acesso">
    Após fazer login, navegue até **Configurações → Integrações → API Keys**. Clique em **Gerar nova chave**, dê um nome descritivo (por exemplo, `integração-erp`) e copie o **Bearer token** exibido.

    <Warning>
      Guarde seu token em um local seguro. Por questões de segurança, ele não poderá ser visualizado novamente após fechar a tela de geração.
    </Warning>
  </Step>

  <Step title="Fazer primeira requisição">
    Com o token em mãos, execute o comando abaixo no seu terminal para consultar o resumo financeiro da sua operação:

    ```bash
    curl -X GET https://api.brudam.com.br/v1/financeiro \
      -H "Authorization: Bearer SEU_TOKEN_AQUI" \
      -H "Content-Type: application/json"
    ```

    Substitua `SEU_TOKEN_AQUI` pelo token copiado na etapa anterior.
  </Step>

  <Step title="Interpretar a resposta">
    Uma resposta bem-sucedida retorna um objeto JSON com o resumo financeiro da sua transportadora. Veja um exemplo:

    ```json
    {
      "saldo": 15240.50,
      "contas_a_receber": 8320.00,
      "contas_a_pagar": 3100.75,
      "competencia": "2025-08"
    }
    ```

    O campo `competencia` indica o mês de referência no formato `AAAA-MM`. Use os campos `contas_a_receber` e `contas_a_pagar` para conciliar seu fluxo de caixa. Para consultar ou atualizar uma entrega específica, utilize o endpoint `GET /v1/delivery/{id}` com o identificador correspondente.
  </Step>
</Steps>

<Note>
  Todas as requisições à API Brudam devem ser feitas via **HTTPS**. Requisições sem o cabeçalho `Authorization` com o Bearer token válido serão rejeitadas com o código `401 Unauthorized`.
</Note>

<Tip>
  Quer explorar todos os endpoints disponíveis, parâmetros de filtragem e modelos de resposta? Consulte a [documentação completa da API](/api-reference/introducao).
</Tip>

## Próximos passos

<CardGroup cols={2}>
  <Card title="Autenticação" icon="key" href="/api-reference/autenticacao">
    Entenda o fluxo de autenticação via Bearer token, rotação de chaves e boas práticas de segurança.
  </Card>
  <Card title="Entregas API" icon="truck" href="/api-reference/entregas">
    Explore todos os endpoints de entregas: criação, atualização de status, ocorrências e comprovantes.
  </Card>
</CardGroup>
